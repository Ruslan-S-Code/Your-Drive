# Your Drive - Car Rental Application

Современное веб-приложение для аренды автомобилей с полноценным backend на Node.js и frontend на React.

> 🇬🇧 [English Version](./README_EN.md) | 🇩🇪 [Deutsche Version / German Version](./README_DE.md)

## 🚀 Быстрый старт

### Требования

- Node.js 18+
- PostgreSQL 12+
- npm или yarn

### Установка и запуск

1. **Установите зависимости:**

```bash
# Frontend зависимости
npm install

# Backend зависимости
cd server
npm install
cd ..
```

2. **Настройте базу данных:**

```bash
cd server

# Создайте файл .env на основе .env.example
cp .env.example .env

# Отредактируйте .env и укажите ваши настройки:
# DATABASE_URL=postgresql://username:password@localhost:5432/yourdrive
# JWT_SECRET=your-super-secret-jwt-key-change-this
# PORT=3001
# FRONTEND_URL=http://localhost:5173

# Создайте базу данных PostgreSQL
createdb yourdrive

# Запустите миграции для создания таблиц
npm run db:migrate

# Заполните тестовыми данными (опционально)
npm run db:seed

cd ..
```

3. **Настройте frontend:**

```bash
# Создайте файл .env в корне проекта
echo "VITE_API_URL=http://localhost:3001/api" > .env
```

4. **Запустите проект:**

```bash
# Запуск backend и frontend одновременно (из корневой папки)
npm run dev:all
```

Или отдельно:

```bash
# Backend (в папке server)
cd server
npm run dev

# Frontend (в корневой папке)
npm run dev
```

- Backend: `http://localhost:3001`
- Frontend: `http://localhost:5173`

## 📁 Структура проекта

```
Your_Drive/
├── server/              # Backend API (Node.js + Express + PostgreSQL)
│   ├── src/
│   │   ├── db/         # База данных и миграции
│   │   ├── routes/     # API маршруты
│   │   ├── middleware/ # Middleware (аутентификация и т.д.)
│   │   └── utils/      # Утилиты
│   └── package.json
├── src/                # Frontend (React + TypeScript + Vite)
│   ├── components/    # React компоненты
│   ├── pages/         # Страницы приложения
│   ├── contexts/      # React контексты
│   ├── lib/           # API клиент и утилиты
│   └── main.tsx       # Точка входа
├── public/            # Статические файлы
└── package.json
```

## 🛠 Технологии

### Backend
- Node.js + Express - собственный backend сервер
- PostgreSQL - локальная или удаленная база данных
- TypeScript - типизированный JavaScript
- JWT аутентификация - собственная реализация
- bcryptjs для хеширования паролей
- multer для загрузки файлов
- nodemailer для email

### Frontend
- React 18
- TypeScript
- Vite
- Tailwind CSS
- React Router
- React Context API
- i18next для интернационализации

## 📋 Основные функции

- 🔐 Аутентификация и регистрация пользователей
- 🚗 Поиск и фильтрация автомобилей
- 📅 Бронирование автомобилей
- 👤 Управление профилем пользователя
- ⭐ Система отзывов
- 🔔 Уведомления о новых событиях, статьях и подкастах
- 🌍 Многоязычность (немецкий/английский)
- 🌓 Темная/светлая тема
- 📱 Адаптивный дизайн

## 🔑 Тестовые данные

После выполнения `npm run db:seed`:
- Email: `test@example.com`
- Password: `password123`

## 📚 API Документация

Полная документация API доступна в [server/README.md](./server/README.md)

### Основные endpoints:

- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/vehicles` - Список автомобилей
- `GET /api/vehicles/:id` - Детали автомобиля
- `POST /api/bookings` - Создать бронирование
- `GET /api/bookings/user/:userId` - Бронирования пользователя
- `GET /api/reviews/vehicle/:vehicleId` - Отзывы для автомобиля

## 🚀 Production сборка

```bash
# Backend
cd server
npm run build
npm start

# Frontend
npm run build
npm run preview
```

## 📝 Переменные окружения

### Backend (.env в папке server/)
- `DATABASE_URL` - строка подключения к PostgreSQL
- `JWT_SECRET` - секретный ключ для JWT
- `PORT` - порт сервера (по умолчанию 3001)
- `FRONTEND_URL` - URL фронтенда
- `SMTP_*` - настройки email для восстановления пароля

### Frontend (.env в корне)
- `VITE_API_URL` - URL backend API (по умолчанию http://localhost:3001/api)

## 🐛 Решение проблем

### Backend не запускается
- Проверьте, что PostgreSQL запущен
- Убедитесь, что DATABASE_URL правильный
- Проверьте, что порт 3001 свободен

### Frontend не подключается к backend
- Убедитесь, что backend запущен на порту 3001
- Проверьте VITE_API_URL в .env файле
- Проверьте CORS настройки в backend

### Ошибки базы данных
- Убедитесь, что миграции выполнены: `npm run db:migrate`
- Проверьте права доступа к базе данных

## 📄 Лицензия

ISC

## 👨‍💻 Автор

Проектирование, backend и frontend — **RSLN**  
🌐 Портфолио: https://www.madebyrsln.com